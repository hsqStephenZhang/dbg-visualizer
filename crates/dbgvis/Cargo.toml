[package]
name = "dbgvis"
version = "0.2.0"
edition = "2024"

[features]
default = []
derive = ["dep:dbgvis-macros"]

[dependencies]
dbgvis-runtime = { path = "../dbgvis-runtime", version = "0.2.0" }
dbgvis-macros = { path = "../dbgvis-macros", version = "0.2.0", optional = true }
